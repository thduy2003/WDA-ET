import React from 'react';
import { CommentData } from '../../../data/CommentData';
const Comment = ({pos}) => {

    return (
        <div>
            
        </div>
    );
};

export default Comment;